.header-inner {
  margin: 0 auto;
  padding: 80px 0 30px;
  width: $desktopMainWidth;
  .one-column & {
    width: $desktopContentWidth;
  }
}

.main-inner {
  margin: 0 auto;
  width: $desktopMainWidth;
  .one-column & {
    width: $desktopContentWidth;
  }

  .content {
    overflow: hidden;
    width: $desktopContentWidth;
  }

  .sidebar {
    float: right;
    margin-left: 20px;
    width: $desktopSidebarWidth;
  }
}

.footer-inner {
  margin 20px auto
  width $desktopMainWidth
  .one-column & {
    width $desktopContentWidth
  }
}
